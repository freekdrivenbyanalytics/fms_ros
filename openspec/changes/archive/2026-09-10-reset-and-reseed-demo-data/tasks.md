## 1. Tripletex client: write support

- [x] 1.1 In `backend/app/tripletex.py`, add `TripletexClient.create_customer(data: dict) -> dict`: POST to `/customer` with the given payload, authenticated via `self._auth()` like the existing GET methods; return the created customer record (including its Tripletex-assigned `id`).
- [x] 1.2 ~~Add `create_delivery_address`~~ — dropped. Verified against Tripletex's live OpenAPI spec and by direct API calls: `/deliveryAddress` has no create endpoint (GET only) and `/deliveryAddress/{id}` has no delete endpoint (GET/PUT only). A delivery address is instead created as a side effect of `create_customer` when its payload includes a `deliveryAddress` object; the response's `deliveryAddress.id` is used directly.
- [x] 1.3 Add `TripletexClient.delete_customer(customer_id: int) -> None`: DELETE `/customer/{customer_id}`.
- [x] 1.4 ~~Add `delete_delivery_address`~~ — dropped for the same reason as 1.2: no such endpoint exists. Deleting the owning customer cascades away its delivery address (confirmed live).

## 2. Bundled demo CSV data

- [x] 2.1 Create `backend/app/demo_data/customers.csv` with ~150 rows: columns `customer_key` (e.g. `cust-001`), `name`, and real, geocodable Oslo/Akershus address fields (street, postal code, city) — roughly 75% Oslo entries, the rest Akershus. Generate this data once now (this is demo content to check in, not code).
- [x] 2.2 Create `backend/app/demo_data/contract_lines.csv` with one row per `customer_key` from 2.1, plus a `product_number` column with one of `TJN10001`, `TJN10002`, `TJN10003`, `TJN10004` assigned at random per row, generated once now (fixed, not re-randomized on each script run).

## 3. Reset script: deletion phase

- [x] 3.1 Create `backend/app/reset_demo_data.py` with a CLI entrypoint (`python -m app.reset_demo_data --confirm`) that, without `--confirm`, prints what it would do and exits without touching Tripletex or the database.
- [x] 3.2 With `--confirm`: fetch all customers from Tripletex (`TripletexClient.get_customers()`) and delete each one via `delete_customer` (this cascades away that customer's delivery address in Tripletex — confirmed live, and there is no standalone delivery-address delete endpoint to fall back to). Do not touch products. A customer whose deletion fails (e.g. Tripletex's built-in sample company, which carries baked-in ledger history) is logged and skipped rather than aborting the run.
- [x] 3.3 Hard-delete, locally, in FK-safe order: `Assignment` rows, then `ServiceVisit` rows, then the `contract_line_products` join table, then `ContractLine` rows, then `Contract` rows, then `CustomerLocationSyncLog` rows, then `CustomerLocation` rows, then `CustomerSyncLog` rows, then `Customer` rows (plain `db.query(Model).delete(synchronize_session=False)` per table, matching the existing bulk-delete pattern used elsewhere in the codebase; the join table needs a raw `db.execute(contract_line_products.delete())` since it has no mapped class). The sync-log tables (`CustomerSyncLog`, `CustomerLocationSyncLog`) were not called out in the original task list but FK-reference `customers`/`customer_locations` with no cascade, so a hard delete fails with a `ForeignKeyViolation` without clearing them first — confirmed live.

## 4. Reset script: Tripletex creation and sync

- [x] 4.1 Read `backend/app/demo_data/customers.csv`; for each row, call `create_customer` with its name and address fields embedded as a `deliveryAddress` object in the same payload (no separate delivery-address create call exists); keep an in-memory map of `customer_key -> tripletex_delivery_address_id` (from the response's `deliveryAddress.id`) for step 5 (not persisted).
- [x] 4.2 After all customers/locations are created in Tripletex, call the existing `sync_customers(db)` then `sync_customer_locations(db)` (from `backend/app/tripletex.py`) to pull the new data into the local database, exactly as `seed.py` and the startup hook already do.

## 5. Reset script: local seeding

- [x] 5.1 Read `backend/app/demo_data/contract_lines.csv`. For each row, resolve its `customer_key` to the synced local `CustomerLocation` (via the Tripletex id captured in 4.1, now present locally after the sync in 4.2), get-or-create that location's `Contract` (one contract per customer, matching `seed.py`'s pattern), and create a `ContractLine` with `start_date = date.today()`, `interval_days = 15`, no `end_date`, `duration_minutes = 45`, and `required_products` set to the single `Product` matching the row's `product_number`.
- [x] 5.2 After flushing each new `ContractLine`, generate its service visits by calling `generate_occurrence_dates` (from `backend/app/visit_generation.py`) with the line's `start_date`, `interval_days`, and `end_date`, and inserting a `ServiceVisit` per resulting date — the same pattern `create_contract_line` in `main.py` uses, not a re-implementation.

## 6. Reset script: employee product portfolio

- [x] 6.1 Query all existing `Employee` rows ordered by `id`. Set every employee's `products` to all `Product` rows matching `TJN10001`-`TJN10004`, except the last employee in that ordering, whose `products` are set to all of those except `TJN10004`.

## 7. Verification

- [x] 7.1 Run the script without `--confirm` against a disposable copy of the database and a non-destructive check of Tripletex reachability; confirm no Tripletex or database writes occur and the script reports confirmation is required.
- [x] 7.2 Run the script with `--confirm` against a disposable Tripletex sandbox/test account (never the real account, unless the user explicitly designates it as disposable) and a disposable database copy; confirm all prior customers/locations are gone from Tripletex, no products were touched, and the local database has ~150 customers, ~150 customer locations, ~150 contracts/contract lines, and generated service visits. Run against the project's connected Tripletex account, confirmed disposable by the user. Verified: 150/150 Tripletex customers created, 25 products untouched, local DB has 150 customers, 150 customer locations, 150 contracts, 150 contract lines, 1050 service visits. Note: the run had to be manually resumed after being killed mid-`sync_customer_locations` when Nominatim's public geocoding API rate-limited this environment (429s) from the volume of testing during this session — the script itself did not fail, it was stopped because every geocode call was hanging; the resumed run used the actual `_seed_contracts_and_visits`/`_update_employee_products` functions unchanged. See 7.3's note on coordinates.
- [x] 7.3 Spot-check several generated contract lines: `interval_days` is 15, no `end_date`, exactly one required product, and generated visits start at today's date and repeat every 15 days out to the existing 90-day open-ended horizon. Verified on 3 sampled lines: interval_days=15, end_date=None, duration_minutes=45, exactly one product each, visits 2026-09-10 through 2026-12-09 every 15 days (7 occurrences, matching the 90-day horizon). Note: all 150 customer locations have null coordinates in this run because Nominatim's public geocoding API rate-limited this environment (429, from repeated test runs earlier in this session) for the entire duration of the run — an external, temporary condition, not a script defect (geocode failures are already handled gracefully, per design). Coordinates will resolve normally the next time the script (or any sync) runs once Nominatim's limit clears; no code change is implicated.
- [x] 7.4 Confirm every existing employee (unaffected by deletion) has the expected product portfolio: all of `TJN10001`-`TJN10004` except the one designated employee, who is missing only `TJN10004`. Verified: all 4 pre-existing employees survived the reset untouched by deletion; 3 have all four demo products, the 4th (John Johnson, last by id) has all but `TJN10004`.
- [x] 7.5 Confirm the Admin Portal's Contracts, Customer Locations, and Products views all reflect the reseeded data correctly (customer names/addresses, contract lines, required products) without further changes needed. Verified live in the browser: Contracts lists all 150 customers with 1 line each and correct product/cadence/visits; Customer Locations lists all 150 with correct addresses (region "—" since geofencing needs coordinates, which are pending the Nominatim rate limit above); Products shows correct per-product employee/contract-line counts.
