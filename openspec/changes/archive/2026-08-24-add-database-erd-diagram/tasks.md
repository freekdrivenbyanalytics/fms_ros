## 1. Schema Review

- [x] 1.1 Re-read `backend/app/models.py` to confirm the current table list, columns, primary keys, foreign keys, and relationship cardinalities (one-to-many, many-to-many).

## 2. Diagram Creation

- [x] 2.1 Build an entity-relationship diagram covering all tables: `regions`, `skills`, `customers`, `customer_locations`, `contracts`, `employees`, `service_visits`, `assignments`, `employee_regions`, `employee_skills`, `contract_skills`.
- [x] 2.2 Show each table's columns with primary keys marked, and connect tables with relationship lines labeled with cardinality (1—N, N—N).

## 3. Publish

- [x] 3.1 Render the diagram as a viewable image (e.g., a Mermaid ER diagram) and publish it as an Artifact so it can be viewed and shared.
- [x] 3.2 Share the resulting link/output with the user for review.
