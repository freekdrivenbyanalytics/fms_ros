## 1. Backend: regeneration on update

- [x] 1.1 In `backend/app/main.py`, add a helper `_is_started(assignment) -> bool` (or reuse/extract the equivalent "started" check already used for the assignment auto-lock, if one exists as a shared function; otherwise `assignment.planned_start <= datetime.now()`).
- [x] 1.2 Add a helper `_regenerate_future_visits(db, line)`: query the line's service visits, split into "has a started assignment" (keep) vs. everything else (remove); delete the "remove" set's assignments then visits (mirroring `_delete_service_visits_for_line`'s delete order, but scoped to this subset); compute the anchor as the max `requested_date` among the "keep" set, or `None`.
- [x] 1.3 In the same helper, compute `horizon = line.end_date or (today + timedelta(days=OPEN_ENDED_HORIZON_DAYS))` once (using a `today = date.today()` captured at the top of the helper). If an anchor exists: `extend_occurrence_dates(anchor, line.interval_days, horizon)`. If no anchor exists: call `generate_occurrence_dates(line.start_date, line.interval_days, horizon)` (passing the computed `horizon`, not `line.end_date` directly, so open-ended lines use today's date for the 90-day window rather than the line's own possibly-long-past `start_date`); then, only if `line.start_date < today` and `line.interval_days > 0` and `horizon >= today`, replace that list with `[today] + [d for d in dates if d > today]` — collapsing any backlog of overdue occurrences into a single visit today while keeping every later occurrence on the original cadence. Insert a new `ServiceVisit` per resulting date.
- [x] 1.4 Call `_regenerate_future_visits(db, line)` from `update_contract_line`, after the line's own fields are updated but before `db.commit()`, so the new visits are generated against the line's already-updated products/duration/interval/dates.

## 2. Frontend: "No end date" checkbox

- [x] 2.1 In `frontend/src/admin-portal/ContractsView.tsx`'s `ContractLineForm`, add a `noEndDate` boolean state, initialized from `existingLine ? existingLine.end_date === null : false`.
- [x] 2.2 Add a checkbox labeled "No end date" above or beside the end-date input.
- [x] 2.3 When `noEndDate` is checked: disable the end-date `<input>` and set its displayed value to `2099-12-31`; when unchecked: re-enable it and restore whatever real date state it had (or empty).
- [x] 2.4 In `handleSubmit`, submit `end_date: noEndDate ? null : (endDate || null)`.

## 3. Verification

- [x] 3.1 Create a contract line, manually assign one of its visits with a `planned_start` in the past (simulating "started"), leave the rest unassigned; update the line's duration; confirm the started visit/assignment is untouched and every other visit for the line was replaced. Verified against disposable DB `fms_ros_cascade_test`, line 29: started visit 97233 (2026-08-01) preserved untouched; all other visits replaced.
- [x] 3.2 Assign and pin a not-yet-started visit on a line; update the line; confirm that visit (and its pin) is gone, replaced by regenerated visits. Verified on line 29: pinned, not-started visit 97240 (2026-09-19) and its assignment were removed; a fresh unassigned visit landed on the same date via the regenerated cadence.
- [x] 3.3 Confirm the regenerated visits' dates continue the cadence from the last-started visit's requested_date (not from today), using the line's (possibly changed) interval_days. Verified on line 29: changed interval_days 7->14; regenerated dates ran 08-15, 08-29, 09-12... from the anchor 08-01, using the new interval, not the old one or today.
- [x] 3.4 Update a contract line whose start_date is today or in the future and has no started visits; confirm the regenerated visits are identical to what creation would have produced (no rollover applies). Verified on line 30 (start_date 2026-09-15, future): regenerated dates identical to creation (09-15, 09-22, 09-29, 10-06).
- [x] 3.5 Update an open-ended line's interval_days; confirm regenerated visits extend to 90 days from *today*, not from the line's original start_date (this is the horizon-computation fix, not just the rollover). Verified on line 31 (start_date 2025-01-06, open-ended): regenerated visits extend to 2026-12-07, ~90 days from today (2026-09-09), not from the original 2025 start_date.
- [x] 3.9 Create a contract line with a start_date well in the past and no visits ever assigned; update one of its fields (e.g. duration); confirm exactly one regenerated visit is dated today, and every subsequent regenerated visit lands on the cadence implied by the *original* start_date and interval_days (e.g. same day-of-week/period alignment), not on a cadence restarted from today. Verified on line 31: first regenerated visit dated today (2026-09-09), subsequent visits on 09-14, 09-21, 09-28 — all Mondays, matching the original 2025-01-06 (Monday) cadence.
- [x] 3.10 Repeat 3.9 for a line whose end_date (or, if open-ended, 90-day-from-today horizon) has already passed relative to today; confirm regeneration produces zero visits rather than one dangling visit today. Found and fixed a bug: the no-anchor branch didn't check `horizon < today` before rolling to today, so an already-elapsed line (end_date 2025-03-01) regenerated its full historical backlog (8 visits) instead of zero. Fixed in `_regenerate_future_visits` by short-circuiting to `occurrence_dates = []` when `horizon < today`; re-verified line 32 now produces 0 visits, and re-ran 3.1-3.5/3.9 to confirm the fix didn't regress them.
- [x] 3.6 Update a bounded line's required products; confirm the regenerated visits, when fetched, show the new required products (read live through the contract line). Verified on line 30: set required_product_ids to [TJN10001]; `GET /service-visits` showed all 4 regenerated visits carrying TJN10001 via their contract_line.
- [x] 3.7 In the Admin Portal, check "No end date" on a line with a real end date, save, and confirm via the API that `end_date` is `null`; reopen the edit form and confirm the checkbox shows checked with `31.12.2099` displayed, disabled. Verified the submit contract (`end_date: noEndDate ? null : (endDate || null)`) end-to-end against the API: PATCH with `end_date: null` round-trips to `end_date: null`. Component wiring (checkbox -> disabled input showing 2099-12-31, `noEndDate` initialized from `existingLine.end_date === null`) verified via code review and a clean `tsc --noEmit -p tsconfig.app.json`; not additionally exercised via live browser click-through, since the frontend dev server defaults to the real backend (`localhost:8000`) and testing there would touch production data.
- [x] 3.8 Uncheck "No end date" on an open-ended line, enter a real date, save, and confirm the line becomes bounded. Verified the submit contract: PATCH line 31 (open-ended) with a real `end_date` correctly made it bounded (`end_date: "2026-12-31"`). Same caveat on live UI click-through as 3.7.
