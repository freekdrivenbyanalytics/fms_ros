## 1. Data Model and Migration

- [x] 1.1 Add `geopy` to `backend/requirements.txt`. (Pinned `geopy==2.4.1`; installed into the backend venv.)
- [x] 1.2 Extend `CustomerLocation` in `backend/app/models.py`: switch `id` to Tripletex-owned (non-autoincrement), add Tripletex-sourced structured address fields (address line 1/2, postal code, city, country, version, url, name), add `delete_flag`, and make `latitude`/`longitude` and `region_id` nullable.
- [x] 1.3 Add `CustomerLocationChangeType` (created/updated/deleted/restored) and a `CustomerLocationSyncLog` model to `backend/app/models.py`, mirroring `CustomerChangeType`/`CustomerSyncLog`.
- [x] 1.4 Create an Alembic migration: truncate `customer_locations` (cascading into `contracts`, `service_visits`, `assignments`), drop `customer_locations.id`'s autoincrement default, drop `region_id`'s NOT NULL constraint, add the new columns, create the `customer_location_sync_log` table and its change-type enum, with a downgrade that reverses it. Verify both `upgrade head` and `downgrade` against the dev DB. (`backend/alembic/versions/0006_tripletex_customer_locations.py` — verified `upgrade head`, `downgrade 0005`, and re-`upgrade head` all succeed.)

## 2. Geocoding

- [x] 2.1 Add a geocoding module wrapping `geopy`'s Nominatim geocoder with a custom user agent and a rate limiter respecting its 1-request/second policy, exposing a function that resolves an address to `(latitude, longitude)` or `None`. (`backend/app/geocoding.py` — verified against the real Nominatim service: a real address resolved to correct coordinates, a bogus address returned `None`.)

## 3. Tripletex Sync

- [x] 3.1 Add a `get_delivery_addresses()` method to `TripletexClient` in `backend/app/tripletex.py`, paginating `GET /deliveryAddress` the same way `get_customers()` paginates `/customer`. (Requests `customerVendor(id)` field expansion so the customer link resolves.)
- [x] 3.2 Add `sync_customer_locations(db)` to `backend/app/tripletex.py`: fetch delivery addresses, skip any without a `customerVendor` or whose `customerVendor.id` isn't a known local customer, and reconcile create/update/soft-delete/restore against locally persisted customer locations, keyed by the delivery address id. (Verified against the real connected Tripletex account: 8 of 9 candidate delivery addresses had a locally-known customer and were synced; the rest correctly excluded.)
- [x] 3.3 Confirm `sync_customer_locations`'s field reconciliation never reads or writes `region_id` — a location's region (or lack of one) must be untouched by create, update, delete, and restore alike. (`_apply_location_fields` and `_LOCATION_SCALAR_FIELD_MAP` never reference `region_id`; verified all 8 synced locations have `region_id: None`.)
- [x] 3.4 In `sync_customer_locations`, geocode each location's address via the module from 2.1 when the location is new or any address field has changed, persisting the resolved coordinates (or leaving them unset if geocoding fails). (7 of 8 real addresses geocoded successfully; one placeholder/fake address correctly got no coordinates. Found and fixed a bug where an initial "retry geocoding whenever still ungeocoded" condition caused spurious `UPDATED` log entries on every idempotent re-sync — removed it so only an actual address change triggers re-geocoding, matching this task's stated trigger exactly. Verified two consecutive re-syncs with no changes produce zero new log entries.)
- [x] 3.5 In `sync_customer_locations`, write a `CustomerLocationSyncLog` entry for each created, updated, deleted, or restored customer location, mirroring how `sync_customers` logs `CustomerSyncLog` entries. (Verified: 8 real syncs each produced exactly one `CREATED` log entry.)

## 4. Backend Integration

- [x] 4.1 Call `sync_customer_locations(db)` immediately after `sync_customers(db)` in `backend/app/main.py`'s startup lifespan.
- [x] 4.2 Call `sync_customer_locations(db)` immediately after `sync_customers(db)` in the existing `POST /customers/sync` endpoint.
- [x] 4.3 Update `backend/app/solver_client.py`'s `build_optimize_payload` to exclude a candidate visit whose customer location has no resolved `latitude`/`longitude`, or no assigned `region_id`, from the schedulable-visits list, and report it in `unscheduled_visit_ids`. (No existing "excluded → reported unscheduled" mechanism existed to reuse — the past-date change reschedules rather than excludes — so `build_optimize_payload` now returns `(payload, excluded_visit_ids)`; `propose_optimization` merges `excluded_visit_ids` into the response's `unscheduled_visit_ids` alongside whatever the solver itself reports. Single call site updated.)
- [x] 4.4 Update `CustomerLocationOut` in `backend/app/schemas.py` with the new structured address fields, and confirm `GET /customer-locations` still excludes deleted (delete_flag) locations by default. (`CustomerLocationOut` gained the structured fields plus nullable `latitude`/`longitude`/`region`. Discovered `list_customer_locations` never actually filtered `delete_flag` — added `.filter(CustomerLocation.delete_flag.is_(False))`, matching `list_customers`' existing pattern, since this change's spec requires it.)

## 5. Seed Data

- [x] 5.1 In `backend/app/seed.py`, replace the hand-created `CustomerLocation` fixtures with a call to `sync_customer_locations(db)` (alongside the existing `sync_customers(db)` call).
- [x] 5.2 Assign a region to each synced customer location: reuse the current seed file's existing per-location region choices (north_holland, utrecht, south_holland, south_holland, in that order) for as many locations as that list covers, and `random.choice` over the existing regions for any location beyond it.
- [x] 5.3 Build the demo contracts against whichever locations come back, dropping any contract fixture that no longer has a location to attach to. (`zip(locations, contract_fixtures)` naturally truncates to whichever list is shorter.)

## 6. Verification

- [x] 6.1 With a delivery address linked to a customer in the connected Tripletex account, run a sync (outside of seeding) and confirm a local customer location is created with fields matching Tripletex, resolved coordinates, and no region assigned. (Verified earlier in task 3.2: a fresh `sync_customer_locations` run against the real account created 8 locations, all with `region_id: None`, 7 with resolved coordinates and 1 correctly ungeocoded.)
- [x] 6.2 Confirm re-running a sync with an unchanged address does not trigger another geocoding call (e.g. by observing no additional geocoder request, or by asserting the stored coordinates are untouched), and does not touch that location's region even if one was later assigned some other way. (Ran `sync_customer_locations` again after seed.py had assigned every location a region: every location's `(region_id, latitude)` pair was byte-for-byte unchanged, and the log entry count stayed at 9.)
- [x] 6.3 Remove a delivery address's customer link (or the address itself) in Tripletex, sync, and confirm the local customer location is marked deleted rather than removed, and its contracts/service visits/assignments remain intact; re-link it and confirm the deleted mark clears without altering its region. (Simulated locally by monkeypatching `get_delivery_addresses` to omit one real location, rather than mutating the live Tripletex account: sync correctly set `delete_flag=True` while preserving `region_id` and leaving its contract in place; a second sync with the full list restored it (`delete_flag=False`) with the same `region_id` unchanged.)
- [x] 6.4 Confirm each of the events in 6.1–6.3 (create, update, delete, restore) produced a matching `CustomerLocationSyncLog` entry with the right change type. (The simulated location's log shows all four: `CREATED`, `UPDATED`, `DELETED`, `RESTORED`, in order.)
- [x] 6.5 Confirm a service visit at a location with no resolved coordinates, and separately one at a location with no assigned region, is excluded from `POST /optimize/propose`'s `scheduled` list and appears in `unscheduled_visit_ids`, while still being viewable and manually assignable via `POST /assignments`. (Visits 20/21 at the ungeocoded "Eksempelstad" location correctly excluded. Temporarily nulled a location's `region_id`: its visits 26/27 — previously scheduled — moved to `unscheduled_visit_ids`; `GET /service-visits` still showed them, and `POST /assignments` manually assigned visit 26 successfully (201). Cleaned up: unassigned visit 26 and restored the region.)
- [x] 6.6 Run the seed script and confirm every customer location it produces a contract for has a region assigned — the seed file's original per-location choices for as many locations as it covers, and a random existing region for the rest. (Ran `python -m app.seed` against the real synced 8 locations: the first 4 (by id order) got North Holland/Utrecht/South Holland/South Holland exactly as the original fixture list specified; the remaining 4 each got a `random.choice` region. All 8 ended up with a region assigned, none left null.)
- [x] 6.7 Confirm the Customer Portal's existing "Refresh" action shows updated customer locations with no frontend code changes, and that Manual Assignment, Day Planning, and Optimize views are otherwise unaffected. (Deviation from the original "no frontend changes" assumption: that was written before region/coordinates became nullable per later feedback, and every view rendering a customer location's `.region.name` or `.latitude`/`.longitude` unguarded would crash on a location still missing either — a real, reachable state now, not hypothetical. Grepped the whole frontend for every such access and added null-safe fallbacks in `AssignedVisitList.tsx`, `UnassignedVisitList.tsx`, `DayPlanningView.tsx`, `CustomerLocationsView.tsx`, `CustomersView.tsx`, `ContractsView.tsx`, and `RegionsView.tsx`; updated `CustomerLocation`'s type to declare `region`/`latitude`/`longitude` as nullable so this is caught by the type system going forward, not just today's grep. Note `tsconfig.app.json` doesn't enable `strictNullChecks`, so `tsc` passing doesn't itself verify null-safety here — the grep sweep is the real check, done manually. All pre-existing read endpoints re-checked and still 200.)
